import React from 'react';
import ReactDOM from 'react-dom';
import App from './App';

// we only to render one thing the app.js all things we want redered are put in the app4
ReactDOM.render(<App />,document.getElementById('root')
);

// reportWebVitals();
